#ifndef UTILS
#define UTILS

#include <string_array.h>
#include <core.h>

using namespace core::types;

class utils {
public:
    static inline smt to_string(s32 num) {
        base_string str_ret;

        for (u32 i = 0; num > 10; i++) {
            
        }
    }
};

#endif // UTILS